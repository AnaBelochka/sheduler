import { AppointmentActionController } from './appointment-action.controller';
import template from './appointment-action.html'

export const AppointmentActionComponent = {
    template,
    controller: AppointmentActionController,
};
